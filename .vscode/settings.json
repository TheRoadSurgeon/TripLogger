{
  "sqltools.connections": [
    {
      "previewLimit": 50,
      "driver": "SQLite",
      "name": "fleet_management_db",
      "database": "${workspaceFolder:TripLogger}/src/fleet_management_db.db"
    }
  ],

  "terminal.integrated.env.windows": {
    "PYTHONPATH": "${workspaceFolder:TripLogger}/src"
  },
  "terminal.integrated.env.linux": {
    "PYTHONPATH": "${workspaceFolder:TripLogger}/src"
  },
  "terminal.integrated.env.osx": {
    "PYTHONPATH": "${workspaceFolder:TripLogger}/src"
  }
}
